apiVersion: v1
kind: Service
metadata:
  name: items
  labels:
    app: items
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: items
    release: {{ .Release.Name }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
