apiVersion: v1
kind: Secret
metadata:
  name: items-db-credentials
type: Opaque
stringData:
  DATABASE_URL: "postgresql://postgres:{{ required "global.postgresPassword must be set via --set" .Values.global.postgresPassword }}@postgres:5432/itemsdb"
