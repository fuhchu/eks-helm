# Override sub-chart values here.
# Keys must match the dependency name exactly.

# The Postgres password is NOT here and is NOT passed via --set. It lives only
# in AWS Secrets Manager; External Secrets Operator pulls it into the cluster.
# Prerequisites for a fresh install: ESO installed + the aws-secrets-manager
# ClusterSecretStore applied (see manifests/cluster-secret-store.yaml).


api-gateway:
  image:
    repository: "445481011516.dkr.ecr.us-west-2.amazonaws.com/eks-helm-api-gateway"
    tag: "v1"

users:
  image:
    repository: "445481011516.dkr.ecr.us-west-2.amazonaws.com/eks-helm-users"
    tag: "v1"

items:
  image:
    repository: "445481011516.dkr.ecr.us-west-2.amazonaws.com/eks-helm-items"
    tag: "v1"
