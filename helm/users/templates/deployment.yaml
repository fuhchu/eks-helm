apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-users
  labels:
    app: users
    release: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: users
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: users
        release: {{ .Release.Name }}
    spec:
      initContainers:
        - name: create-db
          image: postgres:16-alpine
          env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: POSTGRES_PASSWORD
          command:
            - sh
            - -c
            - |
              until pg_isready -h postgres -U postgres; do sleep 2; done
              psql -h postgres -U postgres -tc "SELECT 1 FROM pg_database WHERE datname = 'usersdb'" | grep -q 1 || \
                psql -h postgres -U postgres -c "CREATE DATABASE usersdb;"
      containers:
        - name: users
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8001
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: users-db-credentials
                  key: DATABASE_URL
            {{- range $key, $val := .Values.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 15
            periodSeconds: 20
