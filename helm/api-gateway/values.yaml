replicaCount: 2

image:
  repository: ""   # filled in by CI: <account>.dkr.ecr.<region>.amazonaws.com/eks-helm-api-gateway
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8000

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "250m"
    memory: "256Mi"

env:
  USERS_SERVICE_URL: "http://users:8001"
  ITEMS_SERVICE_URL: "http://items:8002"
